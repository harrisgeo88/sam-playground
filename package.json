{
  "name": "sam-playground",
  "version": "1.0.0",
  "description": "This project contains source code and supporting files for a serverless application that you can deploy with the SAM CLI. It includes the following files and folders.",
  "scripts": {
    "build": "sam build --beta-features",
    "deploy": "sam deploy",
    "sync": "sam sync --beta-features --stack-name sam-playground --watch --region eu-west-1",
    "logs": "sam logs --stack-name sam-playground --tail --region eu-west-1"
  }
}
